[project]
name = "llm-away"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pick"]

[project.scripts]
llm-away = "llm_away.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
