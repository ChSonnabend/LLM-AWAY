Bootstrap: docker
From: nvidia/cuda:12.8.1-devel-ubuntu22.04

%post
    export DEBIAN_FRONTEND=noninteractive
    apt-get update
    apt-get install -y --no-install-recommends build-essential cmake ninja-build git ca-certificates libcurl4-openssl-dev python3
    rm -rf /var/lib/apt/lists/*
